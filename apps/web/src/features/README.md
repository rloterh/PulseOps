# Features

Feature directories will own domain logic, validation, actions, queries, and supporting components. Route files should stay thin and compose feature-level modules instead of accumulating business logic directly in `app/`.
