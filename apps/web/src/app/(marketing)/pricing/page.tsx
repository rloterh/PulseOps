import { RoutePlaceholder } from '@/components/route-placeholder';

export default function PricingPage() {
  return (
    <RoutePlaceholder
      eyebrow="Marketing"
      title="Pricing surface placeholder"
      description="The public pricing surface exists now so billing and commercial UX can slot into a stable route later without route churn."
    />
  );
}
