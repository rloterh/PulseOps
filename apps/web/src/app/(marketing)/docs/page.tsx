import { RoutePlaceholder } from '@/components/route-placeholder';

export default function DocsPage() {
  return (
    <RoutePlaceholder
      eyebrow="Marketing"
      title="Docs surface placeholder"
      description="Sprint 0 includes the route boundary now and the real help center or CMS-backed content can arrive in a later sprint."
    />
  );
}
