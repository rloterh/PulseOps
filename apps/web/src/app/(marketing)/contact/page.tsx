import { RoutePlaceholder } from '@/components/route-placeholder';

export default function ContactPage() {
  return (
    <RoutePlaceholder
      eyebrow="Marketing"
      title="Contact surface placeholder"
      description="This placeholder keeps public conversion routes stable while product and marketing content are still being designed."
    />
  );
}
