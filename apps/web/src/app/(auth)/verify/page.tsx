import { RoutePlaceholder } from '@/components/route-placeholder';

export default function VerifyPage() {
  return (
    <RoutePlaceholder
      eyebrow="Auth"
      title="Verification placeholder"
      description="Verification messaging is stubbed now so the auth callback flow can evolve without reshaping the route tree."
    />
  );
}
