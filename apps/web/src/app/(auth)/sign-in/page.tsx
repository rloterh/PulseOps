import { RoutePlaceholder } from '@/components/route-placeholder';

export default function SignInPage() {
  return (
    <RoutePlaceholder
      eyebrow="Auth"
      title="Sign-in flow placeholder"
      description="Sprint 1 will add the secure Supabase auth flow. Sprint 0 only preserves the route boundary and design language."
    />
  );
}
