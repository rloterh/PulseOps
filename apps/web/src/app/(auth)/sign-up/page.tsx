import { RoutePlaceholder } from '@/components/route-placeholder';

export default function SignUpPage() {
  return (
    <RoutePlaceholder
      eyebrow="Auth"
      title="Account creation placeholder"
      description="Tenant onboarding and invitation flows are intentionally deferred until the auth and tenancy sprint."
    />
  );
}
