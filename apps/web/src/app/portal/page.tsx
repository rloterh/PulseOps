import { RoutePlaceholder } from '@/components/route-placeholder';

export default function PortalPage() {
  return (
    <RoutePlaceholder
      eyebrow="Portal"
      title="Customer portal placeholder"
      description="The client-safe portal route exists now, while the underlying customer identity model and access rules are still being designed."
    />
  );
}
