import { RoutePlaceholder } from '@/components/route-placeholder';

export default function DashboardPage() {
  return (
    <RoutePlaceholder
      eyebrow="Dashboard"
      title="Overview shell placeholder"
      description="The authenticated app shell is in place, ready for KPIs, activity streams, and branch health surfaces in the next sprint."
    />
  );
}
