import { ModulePlaceholder } from '@/components/dashboard/module-placeholder';

export default function TeamPage() {
  return (
    <ModulePlaceholder
      eyebrow="Team"
      title="Team operations are ready for staffing and scheduling views."
      description="Sprint 2 brings the route into the live shell so technician assignments, team capacity, and roster tools can arrive without changing how operators move through the app."
    />
  );
}
