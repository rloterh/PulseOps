import { RoutePlaceholder } from '@/components/route-placeholder';

export default function BranchesPage() {
  return (
    <RoutePlaceholder
      eyebrow="Dashboard"
      title="Branches module placeholder"
      description="Branch-focused routes are named to match the product language, not generic location terminology."
    />
  );
}
