import { RoutePlaceholder } from '@/components/route-placeholder';

export default function BillingPage() {
  return (
    <RoutePlaceholder
      eyebrow="Dashboard"
      title="Billing module placeholder"
      description="Stripe-backed billing will arrive later, but the tenant admin surface is anchored now."
    />
  );
}
