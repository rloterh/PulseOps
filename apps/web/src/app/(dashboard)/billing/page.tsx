import { ModulePlaceholder } from '@/components/dashboard/module-placeholder';

export default function BillingPage() {
  return (
    <ModulePlaceholder
      eyebrow="Billing"
      title="Billing stays reserved for later subscription work."
      description="The route remains accessible inside the protected shell so commercial controls can be added later without introducing dead-end navigation."
    />
  );
}
