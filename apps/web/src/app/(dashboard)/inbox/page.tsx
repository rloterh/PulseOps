import { ModulePlaceholder } from '@/components/dashboard/module-placeholder';

export default function InboxPage() {
  return (
    <ModulePlaceholder
      eyebrow="Inbox"
      title="Inbox will become the unified communications surface."
      description="This route is now part of the authenticated shell and command palette, ready for alerts, messages, and operational communication streams in a later sprint."
    />
  );
}
